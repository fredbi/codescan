* Q: infoExtensionBlock : "---" example?
* add fixture to figure out exactly the production of the various annotations beneath swagger:meta
* distinguish SimpleSchema vs Schema at the grammar level (e.g. parameters, response headers)
* keywords: package grammar2/keywords ? (so this reads more naturally?) 
* tokens: same approach? e.g tokens.EOF rather than grammar2.TokenEOF?
* lexer helpers: move in a distinct package all utilities that do not deal directly with tokens (e.g. blank line handlers etc) 
